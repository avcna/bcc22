import Animation from '../images/loading.gif';
import styled from 'styled-components';


const Loading =()=>{
  return(
    <section>
      <center><img src={Animation}/></center>
    </section>
  )
}

export default Loading;
